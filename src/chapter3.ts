// object
let user: {
    id? : number;
    name : string;
} = {
    id: 1,
    name : '이유정',
};

let config : {
    readonly apiKey : string;
} = {
    apiKey : 'MY API KEY',

};

// config.apiKey = "hacked";